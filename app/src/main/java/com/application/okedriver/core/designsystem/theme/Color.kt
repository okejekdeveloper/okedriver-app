package com.application.okedriver.core.designsystem.theme

import androidx.compose.ui.graphics.Color

// ── Brand Primary — Violet ─────────────────────────────────────────────────────
val OkePrimary          = Color(0xFF7C3AED)   // Violet 600
val OkePrimaryVariant   = Color(0xFF6D28D9)   // Violet 700
val OkePrimaryLight     = Color(0xFFA78BFA)   // Violet 400
val OkePrimaryContainer = Color(0xFFEDE9FE)   // Violet 100
val OkePrimaryDark      = Color(0xFF4C1D95)   // Violet 900

// ── Brand Secondary ───────────────────────────────────────────────────────────
val OkeSecondary          = Color(0xFF8B5CF6)   // Violet 500
val OkeSecondaryContainer = Color(0xFFF5F3FF)   // Violet 50

// ── Login Screen Gradient ─────────────────────────────────────────────────────
val OkeLoginGradientTop    = Color(0xFF9F67FF)
val OkeLoginGradientMid    = Color(0xFF7C3AED)
val OkeLoginGradientBottom = Color(0xFF4C1D95)

// ── Balance Card / Button Gradient ────────────────────────────────────────────
val OkeCardGradientStart = Color(0xFF8B5CF6)
val OkeCardGradientEnd   = Color(0xFF5B21B6)

// ── Action Button Colors ──────────────────────────────────────────────────────
val OkeTopUpGradientStart    = Color(0xFF6D28D9)
val OkeTopUpGradientEnd      = Color(0xFF4C1D95)
val OkeWithdrawGradientStart = Color(0xFF2563EB)
val OkeWithdrawGradientEnd   = Color(0xFF1D4ED8)

// ── Dashboard – Dark Map ──────────────────────────────────────────────────────
val OkeDarkBg      = Color(0xFF08081A)   // Deeper navy
val OkeDarkSurface = Color(0xFF13132B)
val OkeDarkCard    = Color(0xFF1E1E3A)
val OkeDarkPanel   = Color(0xFF181830)
val OkeMapRoad     = Color(0xFF232350)
val OkeMapRoute    = Color(0xFF8B5CF6)

// ── Background & Surface – Light ──────────────────────────────────────────────
val OkeBg        = Color(0xFFF4F4F8)   // Subtle warm grey
val OkeSurface   = Color(0xFFFFFFFF)
val OkeCardBg    = Color(0xFFFFFFFF)

// ── Card / Elevation ──────────────────────────────────────────────────────────
val OkeCardBorder  = Color(0xFFEBEAF0)
val OkeShadowColor = Color(0xFF000000)

// ── Text ──────────────────────────────────────────────────────────────────────
val OkeTextPrimary   = Color(0xFF18181B)   // Near black, warmer
val OkeTextSecondary = Color(0xFF71717A)   // Zinc 500
val OkeTextHint      = Color(0xFFA1A1AA)   // Zinc 400
val OkeTextOnPrimary = Color.White

// ── Input Fields ──────────────────────────────────────────────────────────────
val OkeInputBg     = Color(0xFFF0EDFF)
val OkeInputBorder = Color(0xFFDDD9F5)

// ── Status ────────────────────────────────────────────────────────────────────
val OkeSuccess      = Color(0xFF10B981)   // Emerald 500
val OkeSuccessBg    = Color(0xFFD1FAE5)   // Emerald 100
val OkeDanger       = Color(0xFFEF4444)   // Red 500
val OkeDangerBg     = Color(0xFFFEE2E2)   // Red 100
val OkeWarning      = Color(0xFFF59E0B)   // Amber 500
val OkeWarningBg    = Color(0xFFFEF3C7)   // Amber 100
val OkeGreenOnline  = Color(0xFF10B981)
val OkeGreyOffline  = Color(0xFF71717A)

// ── Route markers ────────────────────────────────────────────────────────────
val OkePickupDot  = Color(0xFF10B981)   // Emerald
val OkeDropoffDot = Color(0xFFEF4444)   // Red

// ── Divider / Neutrals ────────────────────────────────────────────────────────
val OkeDivider    = Color(0xFFF0EFF5)
val OkeMenuItemBg = Color(0xFFF9F8FF)
val OkeSelectedBg = Color(0xFFEDE9FE)

// ── Bank Brand Colors ─────────────────────────────────────────────────────────
val OkeBankBca     = Color(0xFF0066B3)
val OkeBankMandiri = Color(0xFF002F6C)
val OkeBankBni     = Color(0xFFF37021)
val OkeBankBri     = Color(0xFF004C97)
val OkeBankDana    = Color(0xFF108EE9)
val OkeBankOvo     = Color(0xFF4C3494)

// ── Order History & Detail ────────────────────────────────────────────────────
val OkeOrange        = Color(0xFFF97316)   // Orange 500
val OkeOrangeBg      = Color(0xFFFFF7ED)   // Orange 50
val OkeServiceRed    = Color(0xFFDC2626)
val OkeMerchantBg   = Color(0xFFEFF6FF)
val OkeMerchantText = Color(0xFF1D4ED8)
val OkeCashBg       = Color(0xFFD1FAE5)
val OkeCashText     = Color(0xFF065F46)

// ── Glassmorphism ─────────────────────────────────────────────────────────────
val OkeGlassWhite = Color(0x1AFFFFFF)    // 10% white
val OkeGlassBorder = Color(0x33FFFFFF)  // 20% white